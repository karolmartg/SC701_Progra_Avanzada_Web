@model PAW.Models.Catalog

@*

*@

@{
    <div class="container">
        <div class="card">
            
            <div class="card-header">@Model.Identifier</div>
            <div class="card-body">@Model.Name</div>

        </div>
    </div>
}